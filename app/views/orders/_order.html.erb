<div id="<%= dom_id order %>" class="flex my-3 items-center gap-8">
  <span class="text-2xl text-gray-700">Orden #<%= order.id %></span>
  <% if action_name != "show" %>
    <% if order.confirmed %>
      confirmada✅
      <% else %>
      no confirmada🚫
    <% end %>
    |
    <%= order.updated_at.in_time_zone("Central Time (US & Canada)").strftime('%d/%m/%Y - %I:%M %p') %>

    <%= link_to "Ver Orden", order, class: "rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>

    <%#= link_to 'Edit this order', edit_order_path(order), class: "rounded-lg py-3 ml-2 px-5 bg-gray-100 inline-block font-medium" %>
    <hr class="mt-6">
  <% end %>
</div>
